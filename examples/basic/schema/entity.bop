import "../node_modules/@vamp/utils/schema/pool.bop"
import "./tags.bop"

message Entity {
	1 -> guid id;
	2 -> guid root;
	3 -> Tags[] tags;
	4 -> guid parent;
	5 -> guid[] children;
	6 -> Pool health;
}
