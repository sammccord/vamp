{
  "name": "basic",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "generate": "vamp-config generate"
  },
  "dependencies": {
    "@vamp/ecs": "workspace:*",
    "@vamp/utils": "workspace:*",
    "@vamp/worker": "workspace:*",
    "bebop": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@vamp/config": "workspace:*",
    "bebop-tools": "catalog:",
    "typescript": "^5.5.2",
    "wrangler": "^4.75.0"
  }
}
